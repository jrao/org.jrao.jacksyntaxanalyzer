# Compiling

To compile the Jack compiler, run ./gradlew build from the project's root directory

# Running

To run the Jack compiler, run java -jar build/libs/org.jrao.vmtranslator.jar [path-to-jack-file-or-directory]
